name: ExtendedItemView
author: ${mod_authors}
version: ${mod_version}-${plugin_version}
main: de.crafty.eiv.servercompat.EivPlugin

api-version: '1.21'

#required for recipe collection
load: POSTWORLD